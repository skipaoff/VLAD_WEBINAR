"""Голосовой ассистент на сайт — standalone worker.

Разговор идёт речь-в-речь через Gemini Live: один WebSocket, голос и очередь реплик
на стороне модели. База знаний из kb/ уходит в system prompt целиком и byte-in-byte,
поэтому файл с границами физически остаётся последним.

Модуль ни от чего не зависит, кроме своего config.json, kb/ и .env.
Запуск: cd worker && python -m bot start
"""
